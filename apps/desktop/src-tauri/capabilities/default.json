{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window plus the torn-off panel windows (labelled panel-*)",
  "windows": ["main", "panel-*"],
  "permissions": ["core:default", "dialog:default"]
}
